# Database (Neon / Supabase Postgres)
DATABASE_URL=postgresql://user:password@host:5432/db?sslmode=require

# Supabase (auth + storage + tenant lookup in middleware)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Base host for tenant resolution (no tenant on this host)
NEXT_PUBLIC_APP_DOMAIN=localhost:3000

# Stripe (use placeholder values — set real keys in .env.local only)
STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
STRIPE_CONNECT_CLIENT_ID=ca_your_connect_id_here

# Fulfillment (optional for MVP)
PRINTIFY_API_TOKEN=
PRINTFUL_API_KEY=
